/* function initLut (tableData) {
    let orgenizedData = {};
    for(let i = 0; i < tableData[0].length; i++){
        if(typeof tableData[1][i] === 'number') {
            orgenizedData[tableData[0][i]] = [];
        }
    }

    return orgenizedData;
}

let commonTableModule = {
    initLut
}

export default commonTableModule */